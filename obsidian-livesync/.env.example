# CouchDB backend for Obsidian LiveSync. Copy to .env.
# Set on first boot; use a long random password. CouchDB hashes it into its own
# runtime config (gitignored), so no hash ends up in git.
COUCHDB_USER=obsidian
COUCHDB_PASSWORD=CHANGE_ME_long_random
