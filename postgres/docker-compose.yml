
services:
  postgres:
    image: postgres:15
    container_name: postgres
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ../../volumes/postgres:/var/lib/postgresql/data
    networks:
      - backend

networks:
  backend:
    external: true
