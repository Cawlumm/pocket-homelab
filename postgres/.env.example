# Shared Postgres. Copy to .env and set a strong password.
# Nextcloud (and any other stack on the `backend` network) connects here.
POSTGRES_USER=nextcloud
POSTGRES_PASSWORD=CHANGE_ME_strong_random
POSTGRES_DB=nextcloud
